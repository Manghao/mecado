<nav>
    <ul class="navBar">
        <li>
            <a href="#" class="titleNav">Mecado</a>
        </li>
        <li>
            <a href="{{ path_for('index') }}">Home</a>
        </li>
        <li>
            <a href="#">Products</a>
        </li>
        {% if auth %}
            <li>
                <a href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Account
                </a>
                <div>
                    <a href="{{ path_for('account') }}">My account</a>
                    <a href="{{ path_for('logout') }}">Logout</a>
                </div>
            </li>
        {% else %}
            <li>
                <a href="#">Login</a>
            </li>
            <li>
                <a href="{{ path_for('user.register.form') }}">Register</a>
            </li>
        {% endif %}
    </ul>
</nav>